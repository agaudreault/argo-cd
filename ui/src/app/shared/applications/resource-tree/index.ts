// Shared resource tree. The tree renders the managed-resource DAG for both
// Applications and ApplicationSets, so it is a shared component consumed by the
// details shell of either kind.
export {ApplicationResourceTree} from '../../components/resource/application-resource-tree/application-resource-tree';
export type {ResourceTreeNode, ApplicationResourceTreeProps} from '../../components/resource/application-resource-tree/application-resource-tree';
